global:
  scrape_interval: 15s
  evaluation_interval: 15s

storage:
  tsdb:
    out_of_order_time_window: 5m

otlp:
  promote_resource_attributes:
    - service.name
    - service.instance.id
    - session.id

# No scrape configs needed — all data arrives via OTLP from the OTel Collector
